{
  "name": "@zen/tui-advanced",
  "version": "0.0.0",
  "description": "Advanced components for professional TUI applications - Window management, splitters, text editors, charts",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "bunup src/index.ts --format esm --dts --target node --splitting=false --external @zen/runtime --external @zen/signal --external @zen/tui --external chalk --external string-width --external strip-ansi",
    "build:watch": "bunup src/index.ts --format esm --dts --target node --splitting=false --watch --external @zen/runtime --external @zen/signal --external @zen/tui --external chalk --external string-width --external strip-ansi",
    "dev": "bun run --watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "zen",
    "tui",
    "terminal",
    "cli",
    "advanced",
    "window",
    "splitter",
    "fullscreen"
  ],
  "author": "Zen Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SylphxAI/zen.git",
    "directory": "packages/zen-tui-advanced"
  },
  "dependencies": {
    "@zen/runtime": "workspace:*",
    "@zen/signal": "workspace:*",
    "@zen/tui": "workspace:*"
  },
  "devDependencies": {
    "bunup": "^0.15.13",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
